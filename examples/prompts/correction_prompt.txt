CORRECTION PROMPT TEMPLATE

Your previous JSON output contained validation errors. Please correct and return ONLY valid JSON.

VALIDATION ERRORS:
{validation_errors}

YOUR PREVIOUS OUTPUT:
{original_output}

COMMON ISSUES TO FIX:

1. MISSING PROVENANCE:
   - Every item MUST have "provenance" field
   - Format: "ResourceType/id" (e.g., "Condition/123")
   - Must reference actual IDs from input data

2. INVALID DATE FORMATS:
   - Use "YYYY-MM-DD" format or null
   - Examples: "2024-01-15", null
   - Invalid: "January 15, 2024", "2024/01/15"

3. MISSING REQUIRED FIELDS:
   - "display" field required for all items
   - "codes" array required (can be empty)
   - All top-level arrays required

4. INVALID JSON STRUCTURE:
   - Remove markdown formatting (```json```)
   - Ensure proper quotes around strings
   - Use arrays [] even if empty
   - Use null for empty values, not undefined

5. INVALID PROVENANCE FORMAT:
   - Wrong: "123", "Condition-123", "Condition/123/extra"
   - Correct: "Condition/123"

CORRECTION CHECKLIST:
□ All items have valid provenance
□ All dates in YYYY-MM-DD format or null
□ All required fields present
□ Valid JSON syntax
□ Arrays used for lists
□ No markdown formatting

Return ONLY the corrected JSON - no explanations or formatting.